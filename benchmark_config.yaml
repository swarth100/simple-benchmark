reference_module: "src.reference"

user_modules:
  - "src.team01"
  - "src.team02"
  - "src.team03"

benchmarks:
  - function_name: "square"
    max_time: 0.5
    args:
      - name: "side"
        increment: "lambda x: x + 1"
        default: 1

  - function_name: "triangle"
    max_time: 0.5
    args:
      - name: "side"
        increment: "lambda x: x + 1"
        default: 1
